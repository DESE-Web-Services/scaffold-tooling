---
ahoyapi: v2
usage: Extend all projects ahoy commands

commands:
  help:
    cmd: |
      echo
      echo -e "\033[0;32mGlobal Ahoy commands\033[0m"
      echo -e "You may add custom commands to \033[0;32mdeptcms/scaffold-tooling/ahoy/ahoy.yml\033[0m to share with developers in all projects."
      echo -e "Type \`\033[0;32mahoy dese\033[0m\` for a list of custom commmands."
      echo

#  mycustomcommand:
#    cmd: |
#      echo "Anything complex you can do on the command line, you can make available for all developers in your project."
#      git log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit -15
